# Shared by system, user, and explicit config files.
--color=never # Inline comment
--template='literal # text'

[hash] # Section comment
-name '#*'

[space]
-name "space name"

[equals]
-name =

[command]
-exec echo "#literal" \# {} \; # Terminator, followed by comment
